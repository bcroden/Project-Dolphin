<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.projectdolphin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.ClassViewActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.SelectItemToEditActivity" />
        <activity android:name=".activities.CategoryViewActivity" />
        <activity android:name=".activities.SaveCategoryActivity" />
        <activity android:name=".activities.SaveClassActivity" />
        <activity android:name=".activities.SaveAssignmentActivity" />
        <activity android:name=".activities.AssignmentViewActivity"></activity>
    </application>

</manifest>
